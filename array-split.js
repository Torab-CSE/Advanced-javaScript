const numbers=[2,5,7,3,4,5,9];
//splice is use to  remove element and also add element. here first number is use to starting of index ,second number is use  to 'count number of remove' total numbers from parent array.It changes the parent array.
//In splice, After second number,you can put any element that is you want .
//slice is use to pick element but after picking the element from array,parent array is not changed.
//Join is use to joining the element of array with some string or anything or nothing.

const add=numbers.join('a');
console.log(add);
// const pickNum=numbers.slice(3,5);
// const remove=numbers.splice(2,3,10,10);

// console.log(numbers,remove,pickNum);

